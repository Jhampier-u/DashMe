# Pausas de hábito · Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Poder marcar un rango de días como pausa para que no rompan la racha ni bajen el cumplimiento.

**Architecture:** Una pausa no es un estado nuevo: es otra razón para que un día no esté programado. El calendario pasa de ser una cadena a ser un dato con `schedule` y `pausas`, y las seis funciones de `streak.ts` lo reciben. Todo lo demás ya sabe tratar un día no programado.

**Tech Stack:** Next 16.2.12 · Drizzle sobre SQLite · vitest 4.1

**Spec:** `docs/superpowers/specs/2026-07-29-pausas-design.md`

---

## Antes de empezar: lee esto

**Este bloque SÍ toca `streak.ts`.** Los dos anteriores lo protegieron; aquí
cambiarlo es el objetivo. Pero **su lógica no cambia**: sigue haciendo lo mismo y
solo pregunta de otra forma si un día tocaba. Si te ves cambiando cómo cuenta,
párate.

**Ninguna expectativa de `streak.test.ts` cambia de valor.** Son 33 llamadas y la
conversión es mecánica: donde va un `schedule`, ahora va un `Calendario` que lo
contiene. **Si una expectativa cambia de número, es un fallo tuyo**, no del test.

**Quita el tipo viejo, no lo hagas compatible.** La tentación es aceptar
`string | Calendario` para no tocar tantos sitios. No lo hagas: entonces
TypeScript no señalaría los que se te olvidan, y una llamada sin convertir
compilaría y contaría los días de pausa como programados. Que el tipo viejo sea
**imposible** es lo que convierte 14 sitios en una lista que el compilador te da.

**Ninguna migración.** Solo se añade una tabla.

**Y no escribas acentos graves en los comentarios de `schema-sql.ts`.**

---

## Estructura de archivos

| Archivo | Responsabilidad |
|---|---|
| `habitos/lib/calendario.ts` | **Nuevo.** `Calendario`, `Rango`, `estaProgramado` |
| `habitos/lib/calendario.test.ts` | **Nuevo.** |
| `habitos/lib/streak.ts` | Las seis funciones reciben `Calendario` |
| `habitos/lib/streak.test.ts` | Mecánico |
| `habitos/lib/metrics.ts` | `HabitSpec.schedule` → `HabitSpec.calendario` |
| `habitos/lib/pausas.ts` | **Nuevo.** Leer, guardar y borrar |
| `habitos/lib/pausas.test.ts` | **Nuevo.** |
| `core/db/schema-sql.ts`, `habitos/schema.ts` | `habit_pauses` |
| `habitos/lib/{habits,home,stats,quests,mutations,racha-global}.ts` | Componen el calendario |
| `habitos/components/habits/HabitDetail.tsx` | La lista de pausas |
| `habitos/components/habits/HabitRow.tsx` | «En pausa» |

---

### Tarea 1: El calendario, puro

**Files:**
- Create: `src/modules/habitos/lib/calendario.ts`
- Create: `src/modules/habitos/lib/calendario.test.ts`
- Modify: `src/modules/habitos/lib/streak.ts` (solo mover dos funciones y reexportarlas)

- [x] **Paso 1: Escribir el test**

`src/modules/habitos/lib/calendario.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import {
  cal,
  corregirRango,
  enPausa,
  estaProgramado,
  type Rango,
} from "./calendario";

const DIA = 86_400_000;
/** El 0 es jueves 1970-01-01; el 4, lunes. */
const d = (n: number) => new Date(n * DIA);

const TODOS = "1111111";
const LUNES = "0100000";
const rango = (a: number, b: number): Rango => ({ desde: d(a), hasta: d(b) });

describe("enPausa", () => {
  it("sin pausas, nunca", () => {
    expect(enPausa([], d(5))).toBe(false);
  });

  /*
    Los dos extremos ENTRAN. «Del 1 al 15» incluye el 1 y el 15: es como lo dice
    cualquiera, y dejar fuera un extremo es el clásico error de un día que nadie
    nota hasta que le rompe la racha.
  */
  it("los dos extremos entran", () => {
    const p = [rango(10, 12)];
    expect(enPausa(p, d(10))).toBe(true);
    expect(enPausa(p, d(11))).toBe(true);
    expect(enPausa(p, d(12))).toBe(true);
  });

  it("fuera del rango, no", () => {
    const p = [rango(10, 12)];
    expect(enPausa(p, d(9))).toBe(false);
    expect(enPausa(p, d(13))).toBe(false);
  });

  it("con varios rangos vale estar en cualquiera", () => {
    const p = [rango(1, 2), rango(10, 12)];
    expect(enPausa(p, d(11))).toBe(true);
    expect(enPausa(p, d(5))).toBe(false);
  });

  /*
    Solapar es inofensivo —la pregunta es si está en ALGUNA— y por eso no se
    prohíbe: validar contra todas las demás en cada guardado sería trabajo para
    evitar un problema que no existe.
  */
  it("los rangos solapados no molestan", () => {
    const p = [rango(10, 15), rango(12, 20)];
    expect(enPausa(p, d(13))).toBe(true);
    expect(enPausa(p, d(18))).toBe(true);
    expect(enPausa(p, d(21))).toBe(false);
  });

  it("un rango de un solo día funciona", () => {
    expect(enPausa([rango(7, 7)], d(7))).toBe(true);
    expect(enPausa([rango(7, 7)], d(8))).toBe(false);
  });

  it("normaliza al día: una hora dentro del rango cuenta", () => {
    expect(enPausa([rango(10, 10)], new Date(d(10).getTime() + 3_600_000))).toBe(
      true,
    );
  });
});

describe("estaProgramado", () => {
  /*
    LA GARANTÍA DE QUE NADA CAMBIA para quien no use pausas: sin pausas,
    `estaProgramado` es exactamente `isScheduledOn`.
  */
  it("sin pausas es el calendario de siempre", () => {
    expect(estaProgramado(cal(TODOS), d(5))).toBe(true);
    // El 5 es viernes, no lunes.
    expect(estaProgramado(cal(LUNES), d(5))).toBe(false);
    // El 4 es lunes.
    expect(estaProgramado(cal(LUNES), d(4))).toBe(true);
  });

  it("un día en pausa no está programado aunque toque", () => {
    expect(estaProgramado(cal(TODOS, [rango(5, 6)]), d(5))).toBe(false);
  });

  it("fuera de la pausa vuelve a estar programado", () => {
    expect(estaProgramado(cal(TODOS, [rango(5, 6)]), d(7))).toBe(true);
  });

  it("una pausa sobre un día que ya no tocaba no cambia nada", () => {
    expect(estaProgramado(cal(LUNES, [rango(5, 5)]), d(5))).toBe(false);
  });
});

describe("corregirRango", () => {
  /*
    Un rango al revés es un error de dedo, no una intención: se intercambia en
    vez de rechazarse. Rechazarlo obligaría al usuario a adivinar qué escribió
    mal cuando el problema es evidente.
  */
  it("intercambia si viene al revés", () => {
    const r = corregirRango(d(12), d(10));
    expect(r.desde.getTime()).toBe(d(10).getTime());
    expect(r.hasta.getTime()).toBe(d(12).getTime());
  });

  it("deja en paz uno correcto", () => {
    const r = corregirRango(d(10), d(12));
    expect(r.desde.getTime()).toBe(d(10).getTime());
  });

  it("normaliza los dos extremos al día", () => {
    const r = corregirRango(
      new Date(d(10).getTime() + 3_600_000),
      new Date(d(12).getTime() + 7_200_000),
    );
    expect(r.desde.getTime()).toBe(d(10).getTime());
    expect(r.hasta.getTime()).toBe(d(12).getTime());
  });
});
```

- [x] **Paso 2: Ejecutar y verlo fallar**

```bash
cd "/c/PROYECTO JUAMPI"
npx vitest run src/modules/habitos/lib/calendario.test.ts
```

- [x] **Paso 3: Escribirlo**

`src/modules/habitos/lib/calendario.ts`:

```ts
import { normalizeDayKey, weekdayOf } from "./day";

/*
  El calendario de un hábito: qué días toca y qué días está en pausa.

  LA IDEA DE TODO ESTE BLOQUE: una pausa no es un estado nuevo, es otra razón
  para que un día no esté programado. Los catorce sitios que consultan el
  calendario ya saben tratar un día no programado —lo saltan, no lo cuentan y no
  lo consideran un fallo—, así que la pausa entra por esa misma puerta y no hay
  que enseñarles nada.

  Es un DATO y no una función. Un predicado `(dia) => boolean` habría sido más
  elegante y habría dejado a `streak.ts` sin saber qué es un calendario, pero
  `HabitSpec` viaja hacia las gráficas y un objeto con una función dentro deja de
  poder cruzar la frontera de servidor a cliente.
*/

/*
  `sanitizeSchedule` e `isScheduledOn` VIVEN AQUÍ y no en `streak.ts`, aunque
  vinieran de allí.

  El motivo es un ciclo: `estaProgramado` necesita `isScheduledOn`, y `streak.ts`
  necesita `estaProgramado`. En ESM el ciclo funcionaría —son funciones y no se
  ejecutan al cargar— pero es frágil de leer y basta con que alguien añada una
  constante calculada arriba para que se rompa en tiempo de arranque. `streak.ts`
  las reexporta para no romper a quien las importaba de allí.
*/

/** El calendario, saneado: 7 caracteres, dom→sáb. */
export function sanitizeSchedule(s: string | null | undefined): string {
  const t = (s ?? "").trim();
  return /^[01]{7}$/.test(t) ? t : "1111111";
}

export function isScheduledOn(
  schedule: string | null | undefined,
  key: Date,
): boolean {
  return sanitizeSchedule(schedule)[weekdayOf(key)] === "1";
}

/** Rango de días, **los dos extremos incluidos**. */
export type Rango = { desde: Date; hasta: Date };

export type Calendario = {
  /** 7 caracteres dom→sáb, 1 = activo. */
  schedule: string;
  pausas: Rango[];
};

/** Atajo para componer un calendario, sobre todo en tests. */
export function cal(schedule: string, pausas: Rango[] = []): Calendario {
  return { schedule, pausas };
}

/**
 * ¿Ese día cae dentro de alguna pausa?
 *
 * Los dos extremos entran: «del 1 al 15» incluye el 1 y el 15, que es como lo
 * dice cualquiera. Dejar fuera un extremo es el clásico error de un día que nadie
 * nota hasta que le rompe una racha.
 */
export function enPausa(pausas: Rango[], dia: Date): boolean {
  const t = normalizeDayKey(dia).getTime();
  return pausas.some(
    (p) =>
      normalizeDayKey(p.desde).getTime() <= t &&
      t <= normalizeDayKey(p.hasta).getTime(),
  );
}

/**
 * ¿Ese día tocaba?
 *
 * Sin pausas es exactamente `isScheduledOn`, y de eso depende que nada cambie
 * para quien no use pausas.
 */
export function estaProgramado(cal: Calendario, dia: Date): boolean {
  return isScheduledOn(cal.schedule, dia) && !enPausa(cal.pausas, dia);
}

/**
 * Normaliza un rango y lo endereza si viene al revés.
 *
 * Intercambiar en vez de rechazar: un rango invertido es un error de dedo, no una
 * intención, y rechazarlo obligaría al usuario a adivinar qué escribió mal cuando
 * el problema es evidente.
 */
export function corregirRango(desde: Date, hasta: Date): Rango {
  const a = normalizeDayKey(desde);
  const b = normalizeDayKey(hasta);
  return a.getTime() <= b.getTime()
    ? { desde: a, hasta: b }
    : { desde: b, hasta: a };
}
```

- [x] **Paso 4: Verificar**

```bash
npx vitest run src/modules/habitos/lib/calendario.test.ts && npx tsc --noEmit
```

- [x] **Paso 5: Mover las dos funciones**

Borra `sanitizeSchedule` e `isScheduledOn` de `streak.ts` y reexpórtalas:

```ts
export { sanitizeSchedule, isScheduledOn } from "./calendario";
```

`sanitizeSchedule` la usa mucha gente y así no hay que tocar a nadie. **Comprueba
que `streak.test.ts` sigue verde sin cambios** en este paso: aún no se ha cambiado
ninguna firma.

- [x] **Paso 6: Commit**

```bash
git add src/modules/habitos/lib/calendario.ts src/modules/habitos/lib/calendario.test.ts src/modules/habitos/lib/streak.ts
git commit -m "feat(habitos): el calendario pasa a ser un dato con pausas"
```

---

### Tarea 2: `streak.ts` y sus ocho consumidores, de una vez

**Corrección hecha al ejecutar: esta tarea NO carga pausas.** El plan original
mandaba a los consumidores cargarlas aquí, pero la tabla es de la Tarea 3: era un
fallo de orden. Lo que hace esta tarea es que el TIPO llegue a todas partes con
listas de pausas vacías, y con la lista vacía `estaProgramado` es exactamente
`isScheduledOn`, así que **el comportamiento no cambia en nada**. La Tarea 3 las
llena. Sale mejor así: este commit es puro refactor verificable, y el siguiente
es puro comportamiento nuevo.

**Tres archivos no los señala `tsc`.** `metrics.ts`, `quests.ts` y
`racha-global.ts` llaman a `isScheduledOn`, que sigue existiendo y compila: son
justo los que la cabecera avisaba que se colarían. Hay que convertirlos a mano y
comprobarlo con el grep del criterio nº 10.

**No se puede partir.** Quitar el tipo viejo rompe catorce llamadas a la vez, así
que no hay punto intermedio donde el repo esté verde. Es el mismo caso que la
unificación de tareas: un commit grande a propósito, porque partirlo dejaría
commits que no compilan.

Y es lo que hace el arrastre seguro: se quita la sobrecarga de `string`, y
entonces **`tsc` te da la lista exacta** de los sitios que faltan. Recórrela hasta
que quede vacía.

**Files:**
- Modify: `src/modules/habitos/lib/streak.ts`
- Modify: `src/modules/habitos/lib/streak.test.ts`
- Modify: `src/modules/habitos/lib/metrics.ts`
- Modify: `src/modules/habitos/lib/{habits,home,stats,quests,mutations,racha-global}.ts`

- [x] **Paso 1: Cambiar las firmas**

En `streak.ts`, estas cinco pasan de `schedule: string | null | undefined` a
`calendario: Calendario`:

- `previousScheduledDay`
- `computeStreak`
- `computeBestStreak`
- `isCriticalDay`
- `countScheduledDays`

`isScheduledOn` y `sanitizeSchedule` **NO cambian**: son las piezas con las que
`estaProgramado` se construye, y `calendario.ts` las importa.

Dentro de cada una, la línea que preguntaba

```ts
if (isScheduledOn(sched, cursor)) {
```

pasa a

```ts
if (estaProgramado(calendario, cursor)) {
```

y desaparece el `const sched = sanitizeSchedule(schedule)` del principio: ahora lo
sanea `estaProgramado` por dentro.

> **Importación circular:** `calendario.ts` importa `isScheduledOn` de `streak.ts`,
> y `streak.ts` va a importar `estaProgramado` de `calendario.ts`. En ESM eso
> funciona porque son funciones y no se ejecutan al cargar, pero es frágil de
> leer. **Mueve `isScheduledOn` y `sanitizeSchedule` a `calendario.ts`** y que
> `streak.ts` las importe de allí junto con `estaProgramado`. Reexpórtalas desde
> `streak.ts` para no romper a quien ya las importaba de ahí.

- [x] **Paso 2: Los tests, mecánico**

En `streak.test.ts`, envuelve cada `schedule` con `cal(...)`:

```ts
import { cal } from "./calendario";
// …
expect(computeStreak(cal(DAILY), doneSet("2026-07-25"), today)).toBe(1);
```

**Ninguna expectativa cambia de valor.** Si una lo hace, para y revisa el paso 1.

Añade además, al final, la prueba de que las pausas llegan hasta la racha:

```ts
describe("las pausas no rompen la racha", () => {
  it("un hueco en pausa vuelve a unir los dos lados", () => {
    // Hecho el 25 y el 28; el 26 y el 27 en pausa.
    const hechos = doneSet("2026-07-25", "2026-07-28");
    const pausa = [{ desde: key("2026-07-26"), hasta: key("2026-07-27") }];
    expect(
      computeStreak(cal(DAILY, pausa), hechos, key("2026-07-28")),
    ).toBe(2);
  });

  it("sin la pausa, ese mismo hueco corta", () => {
    const hechos = doneSet("2026-07-25", "2026-07-28");
    expect(computeStreak(cal(DAILY), hechos, key("2026-07-28"))).toBe(1);
  });
});
```

> Las dos juntas son la prueba de que la función hace algo: la segunda muestra que
> sin pausa el resultado es distinto. Una sola no demostraría nada.

- [x] **Paso 3: La lista de sitios que faltan**

```bash
npx tsc --noEmit
```

Esperado: **una lista de errores**, uno por cada llamada sin convertir. Es el mapa
de los pasos siguientes.

- [x] **Paso 4: Los ocho consumidores**

Está detallado más abajo, en la sección «Los ocho consumidores», que forma parte
de ESTA tarea. Hazla ahora y vuelve.

- [x] **Paso 5: Verificar y commit, ya en verde**

```bash
npx tsc --noEmit && npm run lint && npx vitest run && npm run build
```

```bash
git add -A src
git commit -m "refactor(habitos): el calendario llega hasta las rachas y las graficas"
```

---

### Tarea 3: La tabla y el acceso

**Files:**
- Modify: `src/modules/core/db/schema-sql.ts`
- Modify: `src/modules/habitos/schema.ts`
- Create: `src/modules/habitos/lib/pausas.ts`
- Create: `src/modules/habitos/lib/pausas.test.ts`

- [x] **Paso 1: El esquema**

En `schema-sql.ts`, tras `habit_notes`:

```sql
CREATE TABLE IF NOT EXISTS habit_pauses (
  id         TEXT PRIMARY KEY,
  habit_id   TEXT NOT NULL REFERENCES habits(id) ON DELETE CASCADE,
  from_day   INTEGER NOT NULL,
  to_day     INTEGER NOT NULL,
  reason     TEXT,
  created_at INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS habit_pauses_habit_idx ON habit_pauses(habit_id);
-- Los rangos pueden solaparse y no se impide: la pregunta que se hace siempre es
-- si un dia cae dentro de ALGUNA pausa.
```

En `schema.ts`:

```ts
export const habitPauses = sqliteTable(
  "habit_pauses",
  {
    id: text("id").primaryKey(),
    habitId: text("habit_id")
      .notNull()
      .references(() => habits.id, { onDelete: "cascade" }),
    /** Clave de día, inclusive. */
    fromDay: integer("from_day", { mode: "timestamp_ms" }).notNull(),
    /** Clave de día, inclusive. */
    toDay: integer("to_day", { mode: "timestamp_ms" }).notNull(),
    reason: text("reason"),
    createdAt: integer("created_at", { mode: "timestamp_ms" }).notNull(),
  },
  (t) => ({ byHabit: index("habit_pauses_habit_idx").on(t.habitId) }),
);

export type HabitPauseRow = typeof habitPauses.$inferSelect;
```

- [x] **Paso 2: El test**

`src/modules/habitos/lib/pausas.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { createTestDb } from "@/modules/core/db/testing";
import { habits, habitLogs } from "@/modules/habitos/schema";
import { addPausa, borrarPausa, pausasDeHabito, pausasPorHabito } from "./pausas";

const T0 = new Date(1700000000000);
const D = (n: number) => new Date(Date.UTC(2026, 6, n));

async function conHabito(id = "h1") {
  const db = createTestDb();
  await db.insert(habits).values({
    id,
    name: "Leer",
    schedule: "1111111",
    createdAt: T0,
  });
  return db;
}

describe("addPausa", () => {
  it("la guarda y la devuelve", async () => {
    const db = await conHabito();
    await addPausa(db, "h1", D(1), D(15), "vacaciones");
    const [p] = await pausasDeHabito(db, "h1");
    expect(p.desde.getTime()).toBe(D(1).getTime());
    expect(p.hasta.getTime()).toBe(D(15).getTime());
    expect(p.reason).toBe("vacaciones");
  });

  it("endereza un rango al revés", async () => {
    const db = await conHabito();
    await addPausa(db, "h1", D(15), D(1), null);
    const [p] = await pausasDeHabito(db, "h1");
    expect(p.desde.getTime()).toBe(D(1).getTime());
    expect(p.hasta.getTime()).toBe(D(15).getTime());
  });

  it("admite varias, incluso solapadas", async () => {
    const db = await conHabito();
    await addPausa(db, "h1", D(1), D(10), null);
    await addPausa(db, "h1", D(5), D(15), null);
    expect(await pausasDeHabito(db, "h1")).toHaveLength(2);
  });

  /*
    Un registro dentro de una pausa NO se borra. Es la única opción que no pierde
    datos, y deshacer la pausa devuelve las cosas a su sitio exactamente.
  */
  it("no borra los registros que caen dentro", async () => {
    const db = await conHabito();
    await db.insert(habitLogs).values({
      id: "l1",
      habitId: "h1",
      date: D(5),
      xpAwarded: 0,
      createdAt: T0,
    });
    await addPausa(db, "h1", D(1), D(10), null);
    expect(await db.select().from(habitLogs)).toHaveLength(1);
  });
});

describe("borrarPausa", () => {
  it("la quita", async () => {
    const db = await conHabito();
    await addPausa(db, "h1", D(1), D(5), null);
    const [p] = await pausasDeHabito(db, "h1");
    await borrarPausa(db, p.id);
    expect(await pausasDeHabito(db, "h1")).toHaveLength(0);
  });

  it("borrar una que no existe no revienta", async () => {
    const db = await conHabito();
    await expect(borrarPausa(db, "fantasma")).resolves.not.toThrow();
  });
});

describe("pausasPorHabito", () => {
  it("agrupa por hábito y no mezcla", async () => {
    const db = await conHabito("h1");
    await db.insert(habits).values({
      id: "h2",
      name: "Correr",
      schedule: "1111111",
      createdAt: T0,
    });
    await addPausa(db, "h1", D(1), D(5), null);
    await addPausa(db, "h2", D(8), D(9), null);

    const mapa = await pausasPorHabito(db);

    expect(mapa.get("h1")).toHaveLength(1);
    expect(mapa.get("h2")![0].desde.getTime()).toBe(D(8).getTime());
  });

  it("un hábito sin pausas no aparece, y eso vale como lista vacía", async () => {
    const db = await conHabito();
    expect((await pausasPorHabito(db)).get("h1") ?? []).toEqual([]);
  });
});
```

- [x] **Paso 3: Ejecutar y verlo fallar**

```bash
npx vitest run src/modules/habitos/lib/pausas.test.ts
```

- [x] **Paso 4: Escribirlo**

`src/modules/habitos/lib/pausas.ts`:

```ts
import { asc, eq } from "drizzle-orm";
import type { Db } from "@/modules/core/db";
import { habitPauses } from "@/modules/habitos/schema";
import { corregirRango, type Rango } from "./calendario";

export const LIMITE_MOTIVO = 120;

export type Pausa = Rango & { id: string; reason: string | null };

export async function pausasDeHabito(db: Db, habitId: string): Promise<Pausa[]> {
  const filas = await db
    .select()
    .from(habitPauses)
    .where(eq(habitPauses.habitId, habitId))
    .orderBy(asc(habitPauses.fromDay));
  return filas.map(aPausa);
}

/**
 * Todas las pausas, agrupadas por hábito.
 *
 * Una consulta y agrupación en memoria, como `listProjects` y `listCategorias`:
 * las pantallas que pintan varios hábitos necesitan las de todos, y una consulta
 * por hábito sería un N+1.
 */
export async function pausasPorHabito(db: Db): Promise<Map<string, Pausa[]>> {
  const filas = await db
    .select()
    .from(habitPauses)
    .orderBy(asc(habitPauses.fromDay));
  const mapa = new Map<string, Pausa[]>();
  for (const f of filas) {
    const lista = mapa.get(f.habitId);
    if (lista) lista.push(aPausa(f));
    else mapa.set(f.habitId, [aPausa(f)]);
  }
  return mapa;
}

export async function addPausa(
  db: Db,
  habitId: string,
  desde: Date,
  hasta: Date,
  reason: string | null,
): Promise<void> {
  if (!habitId) return;
  // Endereza y normaliza: un rango al revés es un error de dedo.
  const r = corregirRango(desde, hasta);
  const motivo = reason?.trim().slice(0, LIMITE_MOTIVO) || null;
  await db.insert(habitPauses).values({
    id: crypto.randomUUID(),
    habitId,
    fromDay: r.desde,
    toDay: r.hasta,
    reason: motivo,
    createdAt: new Date(),
  });
}

export async function borrarPausa(db: Db, id: string): Promise<void> {
  if (!id) return;
  await db.delete(habitPauses).where(eq(habitPauses.id, id));
}

function aPausa(f: {
  id: string;
  fromDay: Date;
  toDay: Date;
  reason: string | null;
}): Pausa {
  return { id: f.id, desde: f.fromDay, hasta: f.toDay, reason: f.reason };
}
```

- [x] **Paso 5: Los server actions**

```ts
export async function crearPausa(
  habitId: string,
  desdeISO: string,
  hastaISO: string,
  reason: string,
) {
  const a = dayKeyFromISO(desdeISO);
  const b = dayKeyFromISO(hastaISO);
  if (!a || !b) return { ok: false as const };
  await pa.addPausa(db, habitId, a, b, reason);
  refresh();
  return { ok: true as const };
}

export async function quitarPausa(id: string) {
  await pa.borrarPausa(db, id);
  refresh();
}
```

Con `import * as pa from "./lib/pausas";`.

- [x] **Paso 6: Commit**

```bash
git add -A src
git commit -m "feat(habitos): la tabla de pausas y su acceso"
```

---

### Los ocho consumidores · parte de la Tarea 2

**Files:**
- Modify: `src/modules/habitos/lib/metrics.ts`
- Modify: `src/modules/habitos/lib/habits.ts`
- Modify: `src/modules/habitos/lib/home.ts`
- Modify: `src/modules/habitos/lib/stats.ts`
- Modify: `src/modules/habitos/lib/quests.ts`
- Modify: `src/modules/habitos/lib/mutations.ts`
- Modify: `src/modules/habitos/lib/racha-global.ts`

- [x] **Paso 1: `metrics.ts`, que es el que arrastra a las gráficas**

`HabitSpec` cambia de campo:

```ts
export type HabitSpec = {
  id: string;
  /** Su calendario, pausas incluidas. */
  calendario: Calendario;
  /** Primer día en que el hábito cuenta: creación o su registro más antiguo. */
  since: Date;
};
```

`buildHabitSpecs` gana un parámetro con las pausas y compone:

```ts
export function buildHabitSpecs(
  habits: HabitRow[],
  logs: LogEntry[],
  pausas: Map<string, Rango[]> = new Map(),
): HabitSpec[] {
```

```ts
      calendario: cal(sanitizeSchedule(h.schedule), pausas.get(h.id) ?? []),
```

Y la línea 85, donde `complianceSeries` pregunta si un día tocaba:

```ts
            estaProgramado(h.calendario, cursor),
```

> Que el parámetro tenga valor por defecto **no es pereza**: `buildHabitSpecs`
> también se llama desde tests que no saben de pausas, y sin pausas el resultado
> es idéntico al de antes. Es lo que hace que sus tests no cambien.

- [x] **Paso 2: `racha-global.ts`**

`EspecieDia.schedule` pasa a `calendario: Calendario`, y `specsVigentes` usa
`estaProgramado`. Como `EspecieDia` ya coincide con `HabitSpec`, el cambio es el
mismo.

- [x] **Paso 3: Los cinco que consultan**

En `habits.ts`, `home.ts`, `stats.ts`, `quests.ts` y `mutations.ts`:

1. Trae las pausas: `pausasPorHabito(db)` en las que miran varios hábitos,
   `pausasDeHabito(db, id)` en las que miran uno.
2. Compón el calendario: `cal(sanitizeSchedule(h.schedule), pausas.get(h.id) ?? [])`.
3. Pásalo donde antes iba el `schedule`.

**La lista exacta te la da `tsc`** del paso 3.
Recórrela hasta que quede vacía.

> En `quests.ts`, `isScheduledOn(h.schedule, today)` decide qué hábitos tocan hoy.
> Con pausas, un hábito pausado **no cuenta para la misión del día perfecto**, y
> eso es correcto: no lo has fallado.

- [x] **Paso 4: Verificar**

```bash
npx tsc --noEmit && npm run lint && npx vitest run && npm run build
```

Esperado: todo verde, y **ninguna expectativa de `streak.test.ts` ni de
`metrics.test.ts` cambiada de valor**.

---

### Tarea 4: La interfaz

**Files:**
- Modify: `src/modules/habitos/lib/habits.ts`
- Modify: `src/modules/habitos/components/habits/HabitRow.tsx`
- Create: `src/modules/habitos/components/habits/PausesPanel.tsx`

- [ ] **Paso 1: «En pausa» en la fila**

`HabitWithStatus` gana `enPausaHoy: boolean`, calculado con
`enPausa(pausasDelHabito, today)`.

En `HabitRow`, cuando está en pausa, en lugar del botón de marcar:

```tsx
{p.enPausaHoy ? (
  <span
    className="label-mono px-2 py-1 rounded-control border-3 border-line bg-paper-2"
    title="Hoy está en pausa: no cuenta ni rompe la racha"
  >
    En pausa
  </span>
) : (
  /* el botón de marcar que ya está en el archivo */
)}
```

> **Sin esto, un hábito pausado parece desaparecido.** No sale como pendiente ni
> cuenta para las misiones, así que hay que decir por qué. Es el criterio nº 9.

- [ ] **Paso 2: El panel de pausas**

`src/modules/habitos/components/habits/PausesPanel.tsx`: una lista con las dos
fechas y el motivo, una ✕ por fila, y un formulario con dos `input type="date"` y
un campo de texto.

```tsx
"use client";

import { useState, useTransition } from "react";
import { crearPausa, quitarPausa } from "@/modules/habitos/actions";
import { Button } from "@/modules/core/ui/Button";
import type { Pausa } from "@/modules/habitos/lib/pausas";

const CAMPO =
  "bg-paper-2 text-tinta font-cuerpo text-[13px] border-3 border-line " +
  "rounded-control px-2 py-1.5 outline-none " +
  "focus:outline-3 focus:outline-offset-2 focus:outline-line";

const iso = (d: Date) => d.toISOString().slice(0, 10);

export function PausesPanel({
  habitId,
  pausas,
}: {
  habitId: string;
  pausas: Pausa[];
}) {
  const [desde, setDesde] = useState("");
  const [hasta, setHasta] = useState("");
  const [motivo, setMotivo] = useState("");
  const [aviso, setAviso] = useState<string | null>(null);
  const [pending, startTransition] = useTransition();

  function crear() {
    if (!desde || !hasta) return;
    startTransition(async () => {
      const r = await crearPausa(habitId, desde, hasta, motivo);
      if (!r.ok) {
        setAviso("Esas fechas no valen.");
        return;
      }
      setDesde("");
      setHasta("");
      setMotivo("");
      /*
        Se avisa SIEMPRE al crear, y no es un adorno: una pausa retroactiva
        vuelve a unir la racha, así que el número puede subir de golpe. Verlo sin
        explicación parece un fallo.
      */
      setAviso("Guardada. Si cubre días pasados, tu racha se ha recalculado.");
    });
  }

  return (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        gap: 10,
        opacity: pending ? 0.6 : 1,
      }}
    >
      {pausas.length === 0 ? (
        <p style={{ fontSize: 13 }}>Sin pausas.</p>
      ) : (
        <ul style={{ listStyle: "none", padding: 0, margin: 0, display: "flex", flexDirection: "column", gap: 6 }}>
          {pausas.map((p) => (
            <li key={p.id} style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 13 }}>
              <span>
                {iso(p.desde)} → {iso(p.hasta)}
              </span>
              {p.reason ? <span style={{ fontSize: 11.5 }}>{p.reason}</span> : null}
              <span style={{ flex: 1 }} />
              <button
                type="button"
                onClick={() => startTransition(() => quitarPausa(p.id))}
                aria-label={`Quitar la pausa del ${iso(p.desde)}`}
                className="px-2 py-0.5 rounded-control border-3 border-line bg-peach text-tinta font-cuerpo text-xs cursor-pointer"
              >
                ✕
              </button>
            </li>
          ))}
        </ul>
      )}

      {aviso ? <p style={{ fontSize: 12.5 }}>{aviso}</p> : null}

      <div style={{ display: "flex", flexWrap: "wrap", gap: 8, alignItems: "center" }}>
        <input
          type="date"
          value={desde}
          onChange={(e) => setDesde(e.target.value)}
          aria-label="Desde"
          className={CAMPO}
        />
        <input
          type="date"
          value={hasta}
          onChange={(e) => setHasta(e.target.value)}
          aria-label="Hasta"
          className={CAMPO}
        />
        <input
          value={motivo}
          onChange={(e) => setMotivo(e.target.value)}
          placeholder="Motivo (opcional)"
          maxLength={120}
          aria-label="Motivo de la pausa"
          className={`${CAMPO} w-40`}
        />
        <Button
          variant="secondary"
          size="sm"
          onClick={crear}
          disabled={pending || !desde || !hasta}
        >
          Añadir pausa
        </Button>
      </div>
    </div>
  );
}
```

> Los dos extremos entran en la pausa, y el rótulo `→` lo sugiere. Si algún día
> se pone un texto explicativo, tiene que decir «incluidos».

- [ ] **Paso 3: Montarlo**

En `HabitDetail.tsx`, una sección más con `<PausesPanel habitId={...} pausas={...} />`.
Las pausas salen de `getHabitStats` o de una lectura propia; expón
`pausasDeHabito` en `index.ts` y úsala desde la página que monta el detalle.

- [ ] **Paso 4: Verificar**

```bash
npx tsc --noEmit && npm run lint && npx vitest run && npm run build
```

- [ ] **Paso 5: Commit**

```bash
git add -A src
git commit -m "feat(habitos): panel de pausas y aviso de en pausa en la fila"
```

---

### Tarea 5: Verificación final

- [ ] **Paso 1: Los cuatro comandos**

```bash
npm run lint && npx tsc --noEmit && npm run test && npm run build
```

- [ ] **Paso 2: Que no quede ningún `isScheduledOn` suelto**

```bash
grep -rn "isScheduledOn(" src/modules/habitos --include=*.ts --include=*.tsx | grep -v calendario
```

Esperado: **nada**, o solo reexportaciones. Cada decisión de «¿tocaba este día?»
tiene que pasar por `estaProgramado`.

- [ ] **Paso 3: Que ninguna expectativa haya cambiado**

```bash
git diff main -- src/modules/habitos/lib/streak.test.ts | grep "^[-+].*toBe(" | sort | uniq -c | sort -rn | head
```

Cada `toBe(...)` que aparezca con `-` debe aparecer también con `+` y el mismo
valor. Si alguno cambia de número, es un fallo.

- [ ] **Paso 4: En pantalla**

1. En un hábito con racha, añade una pausa que **cubra hoy**: la fila dice «En
   pausa» y el hábito sale de los pendientes.
2. Quita la pausa: vuelve como estaba.
3. Añade una pausa sobre días pasados que fallaste y comprueba que la racha
   **sube** y que aparece el aviso.
4. Marca un día, luego pausa ese día: el registro **sigue** en la base.
5. Mete las fechas al revés y comprueba que se guardan enderezadas.

- [ ] **Paso 5: Marcar el plan y el spec**

---

## Criterios de aceptación

- [ ] 1. `build`, `test`, `lint` y `tsc --noEmit` en verde
- [ ] 2. Un hábito sin pausas se comporta **exactamente** como hoy
- [ ] 3. Un día en pausa no rompe la racha
- [ ] 4. Un día en pausa no baja el porcentaje de cumplimiento
- [ ] 5. Una pausa retroactiva vuelve a unir la racha
- [ ] 6. Un registro dentro de una pausa **no se borra**
- [ ] 7. Los rangos al revés se corrigen al guardar
- [ ] 8. Ninguna expectativa de `streak.test.ts` cambia de valor
- [ ] 9. La fila dice «En pausa» cuando hoy lo está
- [ ] 10. No queda ningún `isScheduledOn` fuera de `calendario.ts`
