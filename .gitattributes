# Normaliza finales de línea: LF en el repo, lo que toque en el disco.
# Sin esto, cada commit escupe un aviso por archivo en Windows.
* text=auto eol=lf
*.png binary
*.ico binary
*.jpg binary
*.db binary
